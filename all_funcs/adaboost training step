 ada = AdaBoostClassifier(
    estimator=DecisionTreeClassifier(max_depth=depth_var),
    n_estimators=estimators_numbers_var,
    algorithm='SAMME',
    learning_rate=learning_rate_var,
    train_test_random_state=42
)

ada.fit(X_train, y_train)
